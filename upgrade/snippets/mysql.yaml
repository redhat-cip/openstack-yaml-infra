# TODO(Emilien): write a script to detect who is the master
# and then restart the right service
- name: restart mysql cluster
  service: name=mysql-bootstrap state=restarted
  tags: 6
  when: inventory_hostname == groups['fakehostgroup'][-1]

